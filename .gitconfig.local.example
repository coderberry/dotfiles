# Copy to ~/.gitconfig.local, then edit. This file is gitignored.
#
#   cp .gitconfig.local.example ~/.gitconfig.local
#   $EDITOR ~/.gitconfig.local

[user]
	name = Your Name
	email = you@example.com
	# signingkey = ...        # if you sign commits

# [commit]
#	gpgsign = true            # uncomment to sign every commit

# Per-directory identity overrides (useful for work vs personal):
# [includeIf "gitdir:~/Code/work/"]
#	path = ~/.gitconfig.work
